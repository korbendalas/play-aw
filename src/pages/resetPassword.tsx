export const ResetPasswordPage = () => {
  // if there are no token as url param, redirect user to homepage
  // if there is a token as url param, render the reset password form

  return (
    <div>
      <h1>Reset Password Page</h1>
    </div>
  );
};
